id: task-2
available: true
wait_seconds: 100
items:
  - is_running: true
    need_delete: true
    k8s_yaml_content: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: n223
      spec:
        replicas: 1
        selector:
          matchLabels:
            app: nginx
        template:
          metadata:
            labels:
              app: nginx
          spec:
            containers:
            - name: nginx
              image: nginx:latest
              imagePullPolicy: Never
              ports:
              - containerPort: 80
  - is_running: true
    need_delete: true
    k8s_yaml_content: |
      apiVersion: v1
      kind: Service
      metadata:
        name: n223
      spec:
        selector:
          app: nginx
        ports:
          - protocol: TCP
            port: 80
            targetPort: 80
        type: ClusterIP